# Azure infrastructure configuration
# These values are specific to your deployment environment and should remain static

# Resource configuration
RESOURCE_GROUP="nobsmed"
LOCATION="westus"
CONTAINER_APP_ENVIRONMENT="prod"
CONTAINER_APP_NAME="nobswebsite"

# Container registry settings
IMAGE_REPO="nobs_backend_amd64"
ACR_NAME="nobsregistry"
REGISTRY_NAME="nobsregistry"

# Identity configuration
IDENTITY="nobsmed-identity"
IDENTITY_ID="nobsmed-identity"

# Dynamic variables (API_VERSION, DB_ENV) are now passed as CLI arguments to deployment scripts
# IMAGE_NAME, AZURE_SEARCH_SERVICE_NAME, and AZURE_SEARCH_SERVICE_ENDPOINT are constructed by scripts